apply plugin: "com.diffplug.spotless"

spotless {
    kotlin {
        target "**/*.kt"
        ktlint().editorConfigOverride([indent_size: 2])
        trimTrailingWhitespace()
        indentWithSpaces()
        endWithNewline()
    }
}
