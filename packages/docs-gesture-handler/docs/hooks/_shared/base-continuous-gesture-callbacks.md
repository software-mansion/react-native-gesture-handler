### Callbacks common to all continuous gestures:

### onUpdate

```ts
onUpdate: (event: HandlerData) => void
```

Set the callback that is being called every time the gesture receives an update while it's active.
