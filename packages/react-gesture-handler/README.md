# react-gesture-handler

Plain React DOM binding of the gesture-handler core: the v3 hook API
(useTapGesture, usePanGesture, ...) and GestureDetector for React web apps,
with no react-native dependency.
